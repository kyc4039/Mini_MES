# 개발 일지

이 프로젝트를 만들면서 실제로 부딪혔던 문제들을 진행 순서대로 정리했다. "처음부터
완벽하게 설계했다"보다, **문제를 어떻게 발견하고 왜 그렇게 고쳤는지**를 남기는 게
더 의미 있다고 생각해서 기록해둔다. 완성된 코드만 봐서는 안 보이는, 시행착오
과정을 최대한 구체적으로 남기려고 했다.

---

## 1일차 — 스키마 설계와 대시보드 골격

### 라면공장 예제에서 배터리 도메인으로

강사님이 만든 라면공장 MiniMes를 그대로 따라 하지 않고, 처음부터 스키마를
다시 설계했다. 라면공장은 단일 공정(포장)이라 테이블 구조가 단순했는데,
배터리는 12단계 공정과 N:1/1:N 계보 관계가 있어서 처음부터 이걸 감당할 수
있는 구조가 필요했다.

**최종적으로 정착한 원칙:**
- 마스터 데이터(`item`, `process`, `equipment`, `worker`, `inspection_spec`)와
  트랜잭션 데이터(`work_order`, `lot`, `lot_genealogy`, `production_result`,
  `inspection_result`)를 명확히 분리
- `production_result` 테이블 하나로 **12개 공정 전부**를 처리 (공정마다 테이블을
  따로 만들지 않고 `process_id`로 구분)
- `lot_genealogy`가 부모-자식 쌍을 기록하는 것만으로 N:1, 1:N, 1:1 관계를
  전부 표현할 수 있다는 걸 확인하고 이 구조로 확정

### 라이트 테마로 전환하면서 CSS 여백이 계속 어긋남

`st.subheader()`와 `st.markdown()`을 섞어 쓰니 Streamlit이 엘리먼트 사이에
자동으로 넣는 여백 때문에 카드마다 위아래 공백이 들쭉날쭉했다.

```python
# 문제가 있던 방식 — 서로 다른 엘리먼트라 여백이 따로 계산됨
st.subheader("설비 현황")
st.markdown(rows_html, unsafe_allow_html=True)
```

제목까지 포함해서 카드 내용 전체를 **HTML 문자열 하나로 합쳐 `st.markdown()`을
한 번만 호출**하는 방식으로 통일하니 해결됐다.

```python
# 개선된 방식 — 하나의 엘리먼트라 Streamlit이 임의로 여백을 안 끼워넣음
content_html = '<p style="font-size:15px;font-weight:600;">설비 현황</p>' + rows_html
st.markdown(content_html, unsafe_allow_html=True)
```

이후 모든 카드가 이 패턴을 따르게 됐다.

### 리스트 항목이 많아지면 카드 높이가 제멋대로 커짐

"설비 현황", "최근 생산실적" 같은 목록형 카드는 데이터가 늘어날 때마다 카드
크기가 같이 커지는 문제가 있었다. `st.container(height=N)`으로 고정 높이 +
스크롤을 적용해서, 데이터가 아무리 늘어도 레이아웃이 안 흔들리게 했다. 처음엔
커스텀 CSS로 `overflow-y`를 직접 지정하려 했는데, Streamlit이 이미 `height`
파라미터로 스크롤 컨테이너를 기본 제공한다는 걸 알고 그걸로 바꿨다.

### KPI 카드에서 반복해서 겪은 사소한 버그들

- `keys()`를 `key()`로 잘못 써서 `AttributeError` — 파이썬 딕셔너리 메서드는
  복수형이라는 걸 다시 확인
- 삼항 조건식에서 `==` 대신 `=`를 써서 문법 오류 — 대입과 비교 연산자 혼동
- f-string 앞에 `f`를 빼먹어서 `{변수명}`이 그대로 텍스트로 출력됨
- 함수를 파일 안에서 두 번 정의해서, 나중 버전이 조용히 앞 버전을 덮어써버림
  (파이썬은 에러 없이 그냥 마지막 정의만 씀 — 디버깅하며 새로 배운 특성)

**공통적으로 배운 점:** 이런 실수들은 전부 "당연히 잘 되겠지"하고 넘어간
부분에서 나왔다. 특히 함수 중복 정의는 에러조차 안 나기 때문에, 화면에
반영이 안 될 때 "혹시 같은 함수가 파일에 두 번 있나?"를 체크하는 습관이
생겼다.

---

## 2일차 — 공정 실적 등록 페이지 (극판/조립/화성)

### 투입 LOT 목록에 엉뚱한 공정의 결과물이 섞여 나옴

처음엔 "품목 카테고리가 같으면" 투입 가능하다고 판단했다.

```python
# 처음 방식 — item_code 접두사만 확인
available_lots_df = all_lots_df[
    all_lots_df["item_code"].apply(lambda c: matches_prefix(c, "CELL-EV"))
]
```

이러면 화성충방전 단계에서, 예전에 이미 화성충방전을 거친 셀이나 심지어
최종검사까지 끝난 셀까지 "CELL-EV 계열"이라는 이유로 다시 후보로 뜨는 문제가
있었다. 처음엔 "그 LOT을 만든 공정 이름"을 라벨에 표시만 해주는 걸로 절반만
고쳤는데, 사용자가 여전히 잘못된 걸 고를 수 있다는 근본 문제는 안 풀렸다.

**"정확히 바로 직전 공정에서 나온 것만 인정"**하는 `predecessors` 제약을
레시피에 추가해서 최종적으로 해결했다.

```python
"FORM-01": {
    "input_prefixes": ["CELL-EV"],
    "output_prefix": "CELL-EV",
    "predecessors": {"CELL-EV": "AGE-01"},  # 정확히 에이징에서 나온 것만
},
```

스태킹처럼 투입 재료가 2종류(재공품+원자재)인 경우엔, 재공품(`WIP-CATHUNIT`)만
직전공정 제약을 걸고 원자재(`RAW-SEPARATOR`)는 제약 없이 열어뒀다. 원자재는
"어느 배치에서 왔든" 상관없이 다 유효한 재고이기 때문이다.

### 투입 재료 중 하나만 재고가 없어도 등록 폼이 그냥 열려버림

스태킹은 양극판+분리막 둘 다 필요한데, 양극판 재고가 0이어도 분리막만 있으면
`available_lots_df`가 비어있지 않아서 폼이 그냥 열렸다. 사용자 입장에서는
"투입 LOT 목록에 왜 분리막밖에 없지?"라는 혼란만 남았다.

카테고리별로 재고 유무를 따로 확인해서, 부족한 품목명을 정확히 짚어주는 경고
문구를 추가했다.

```python
def missing_input_categories(all_lots_df, recipe) -> list[str]:
    missing = []
    for prefix in recipe["input_prefixes"]:
        matched = all_lots_df[...]  # 카테고리 + 직전공정 조건까지 확인
        if matched.empty:
            missing.append(prefix)
    return missing
```

### 투입 LOT 재고보다 많은 양을 넣어도 그냥 저장됨

생산실적 등록 로직이 `qty_used > 0`인지만 확인하고, 실제 남은 재고량을 넘는지는
확인하지 않았다. 잔여수량이 음수로 떨어질 수 있는 진짜 버그였다.

저장 트랜잭션 맨 앞에서 각 투입 LOT의 실제 잔여수량을 먼저 조회해서 초과분을
막도록 수정했다. 걸리면 어떤 INSERT도 실행되기 전에 에러가 나서, 트랜잭션
컨텍스트매니저가 자동으로 롤백한다 — 격리 테스트로 "초과 투입 시 아무것도
안 저장됨"과 "정상/경계값 투입은 그대로 성공함"을 둘 다 확인했다.

### 극판/조립/화성 세 페이지에 거의 동일한 규칙 코드가 복사돼 있었음

공정별 투입/산출 규칙, 결과수량 계산, LOT번호 생성 로직이 세 파일에 거의
똑같이 중복돼 있어서, 하나를 고치면 나머지 두 곳도 일일이 찾아 고쳐야 했다.
실제로 조립·화성 페이지에 `PROCESS_ENGLISH_TAG`가 완전히 똑같은 내용으로
중복돼 있는 걸 통합하면서 발견했다.

`src/process_rules.py` 하나로 통합했다. `process_code`가 스키마 전체에서
유일한 값이라는 걸 이용해서, 12개 공정 규칙을 딕셔너리 하나로 관리하게 했다.

---

## LOT 번호 체계 — 세 번 바뀐 이유

셀(CELL) LOT 번호를 어떻게 붙일지 세 번 바뀌었다.

1. **`CELL-SN-2026072201`** (날짜+순번을 붙여쓰기) — 처음엔 이렇게 했는데,
   자릿수만 보고는 날짜와 순번의 경계가 눈에 안 들어와서 가독성이 떨어졌다.
2. **`CELL-SN-{날짜}-{순번}`**로 하이픈 추가 — 훨씬 읽기 편해졌는데, 이건
   "몇 번째 셀인지"는 알아도 "어느 공정에서 나왔는지"는 LOT번호만 봐서 알
   수 없다는 한계가 남아 있었다.
3. **`CELL-SN-{공정영문태그}-{날짜}-{그 공정·그날 순번}`**
   (예: `CELL-SN-STACK-260805-01`)로 최종 확정 — 공정명을 접두사에 포함시키니
   "이 LOT번호를 세는 기준(prefix)" 자체가 공정별로 나뉘는 부수효과도 얻었다.
   즉 순번 계산 로직을 따로 안 만들어도, 접두사가 다르면 자동으로 카운트가
   갈렸다.

---

## 대시보드 공정 파이프라인 실시간화

### "몇 개 공정을 완료했는지" 세는 방식의 함정

처음엔 "이 작업지시에서 이 공정을 한 번이라도 수행했는가"로 완료 여부를
판단했다.

```python
completed_ids = {
    row[0] for row in connection.execute(
        "SELECT DISTINCT process_id FROM production_result WHERE work_order_id = ?",
        (work_order_id,),
    ).fetchall()
}
```

그런데 오늘 새로 믹싱을 시작해도, 예전에 이미 끝낸 코팅~최종검사가 계속
"완료"로 표시되는 문제가 있었다. **"오늘 새로 시작한 3번째 셀"과 "어제 이미
끝난 1·2번째 셀"을 구분하지 못했기 때문**이다. 실제로 방금 믹싱을 시작했는데
나머지 11개 공정이 전부 초록색(완료)으로 뜨는 걸 보고서야 문제를 알아챘다.

공정별로 따로 비교하지 않고, **"이 작업지시에서 가장 최근에 시작된 실적이
몇 번째 공정인지" 하나만 먼저 찾고**, 그 위치보다 앞은 완료·그 자신은 시간
비교·뒤는 대기로 판정하는 방식으로 다시 설계했다.

```python
latest_overall = connection.execute(
    """
    SELECT pr.process_id, pr.start_time, pr.end_time, p.seq_no
    FROM production_result AS pr JOIN process AS p ON pr.process_id = p.process_id
    WHERE pr.work_order_id = ? ORDER BY pr.start_time DESC LIMIT 1
    """,
    (work_order_id,),
).fetchone()
```

가장 최근에 시작된 실적이 있는 공정의 `seq_no`를 기준점으로 삼고, 그보다
순서가 앞인 공정은 완료, 뒤는 대기로 판정하니 문제가 정확히 해결됐다.
격리된 시나리오(어제 완료된 기록 + 오늘 새로 시작한 믹싱)로 재현 테스트를
해서 확인했다.

---

## 자잘하지만 자주 겪은 위젯 관련 실수

- **위젯 값이 계속 "지금 시각"으로 리셋됨** — `value=datetime.now()`처럼 매번
  바뀌는 값을 `key` 없이 위젯에 넘기면, 다른 위젯을 조작해서 화면이 재실행될
  때마다 값이 초기화됐다. `st.session_state`에 값을 한 번만 저장하고
  `key`를 명시해서 해결했다.
- **`RecursionError: maximum recursion depth exceeded`** — `st.Page()`로
  네비게이션을 구성할 때, 진입점 파일이 자기 자신을 페이지 목록에 넣어서
  무한 재귀가 발생했다. 진입점 파일(`main.py`)과 페이지 내용 파일을 분리해서
  해결했다.
- **`st.form` 안에서 동적 위젯이 안 늘어남** — 투입 LOT을 여러 개 선택하면
  그 개수만큼 수량 입력칸이 생겨야 하는데, `st.form` 안에서는 내부 위젯을
  조작해도 폼이 제출되기 전까지 스크립트가 재실행되지 않아서 동적으로 칸이
  안 늘어났다. 폼을 아예 안 쓰고 일반 위젯 + 버튼 조합으로 바꿔서 해결했다.

---

## 마무리 정리 — 배포 전 리팩토링

시간을 정해두고 마지막에 아래 순서로 정리했다.

1. **재고 초과 투입 방지** — 위에서 설명한 버그 수정
2. **극판공정에도 직전공정 검증 추가** — 조립/화성 페이지엔 이미 있던 검증이
   극판공정 페이지엔 빠져 있었던 걸 발견하고 통일
3. **공정 규칙 통합** (`src/process_rules.py`) — 세 페이지의 중복 코드 정리
4. **작업지시 관리 페이지 신규 제작** — 지금까지는 작업지시를 SQL로 직접
   넣어야 했는데, 화면에서 새 작업지시를 등록할 수 있는 페이지를 추가해서
   "계획→실행→추적"이 전부 화면 안에서 닫히게 했다

---

## GitHub에 처음 올리며 겪은 것들

- **`.gitignore` 없이 시작했으면 DB 파일까지 통째로 올라갈 뻔했다** — `.db`
  파일은 제외하고, 대신 `sqlite3 db.db .dump > sql/dump.sql`로 스키마+데이터를
  텍스트로 뽑아서 그걸 커밋했다. 이러면 DB 파일 자체 없이도 누구나 같은
  상태를 재현할 수 있다.
- **일반 비밀번호로 `git push`가 거부됨** — GitHub은 더 이상 계정 비밀번호로
  push를 허용하지 않는다. Personal Access Token을 따로 발급받아서 비밀번호
  자리에 입력해야 했다.
- **`git commit --amend`는 "커밋 수정"이 아니라 "커밋 교체"** — 커밋 메시지를
  한 번 고치려다가, 이미 push된 커밋을 amend하면 로컬과 원격이 "서로 다른
  버전"으로 갈라진다는 걸 (`have diverged` 메시지로) 직접 겪었다. 이럴 땐
  `git push --force`로 원격을 로컬 기준으로 덮어써야 한다는 걸 배웠다 — 여러
  명이 같이 쓰는 레포에서는 위험하지만, 혼자 쓰는 레포에서는 안전한 선택이다.

---

## 돌아보며

이 프로젝트에서 가장 크게 배운 건, **"일단 되게 만드는 것"과 "제대로 되게
만드는 것" 사이의 간극**이었다. 특히 LOT 필터링 문제는 겉보기엔 잘 동작하는
것처럼 보였는데, 실제로 여러 작업지시·여러 날짜의 데이터가 쌓이고 나서야
허점이 드러났다. 데이터가 하나뿐일 때는 안 보이던 버그가 데이터가 늘어나면서
드러나는 경험을 여러 번 했다.

또 하나 배운 건, **버그를 고칠 때 "그냥 되게" 고치는 것과 "왜 그런 문제가
생겼는지 원리를 이해하고" 고치는 것의 차이**였다. 파이프라인 실시간화 문제가
대표적인데, 처음 시도(공정별로 개별 시간 비교)는 겉보기엔 그럴듯했지만 여러
번의 생산 사이클이 쌓이는 상황을 놓쳤다. 문제를 다시 정의하고 나서야 "가장
최근 활동 지점"이라는 더 견고한 기준을 찾을 수 있었다.

마지막으로, 처음엔 페이지마다 코드를 복사-붙여넣기 하면서 빠르게 기능을
늘려갔는데, 나중에 규칙을 한 곳(`process_rules.py`)으로 모으는 리팩토링을
하면서 **"일단 동작하게 만드는 속도"와 "유지보수 가능한 구조를 만드는
비용"** 사이에서 균형을 잡는 경험을 했다.