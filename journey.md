# 개발 일지 — 겪은 문제와 해결 과정

이 프로젝트를 만들면서 실제로 부딪혔던 문제들을 시간 순서대로 정리했다.
"처음부터 완벽하게 설계했다"보다, "문제를 어떻게 발견하고 왜 그렇게 고쳤는지"를
남기는 게 더 의미 있다고 생각해서 기록해둔다.

## 1단계 — 대시보드 기본 골격

### 라이트 테마로 전환하면서 CSS 여백이 계속 어긋남
`st.subheader()`와 `st.markdown()`을 섞어 쓰니 Streamlit이 엘리먼트 사이에
자동으로 넣는 여백 때문에 카드마다 위아래 공백이 들쭉날쭉했다.
→ 제목까지 포함해서 카드 내용 전체를 **HTML 문자열 하나로 합쳐 `st.markdown()`을
한 번만 호출**하는 방식으로 통일하니 해결됐다. 이후 모든 카드가 이 패턴을 따르게 됐다.

### 리스트 항목이 많아지면 카드 높이가 제멋대로 커짐
"설비 현황", "최근 생산실적" 같은 목록형 카드는 데이터가 늘어날 때마다 카드
크기가 같이 커지는 문제가 있었다.
→ `st.container(height=N)`으로 고정 높이 + 스크롤을 적용해서, 데이터가 아무리
늘어도 레이아웃이 안 흔들리게 했다.

## 2단계 — 공정 실적 등록 페이지 (극판/조립/화성)

### 투입 LOT 목록에 엉뚱한 공정의 결과물이 섞여 나옴
처음엔 "품목 카테고리가 같으면" 투입 가능하다고 판단했는데, 이러면 예를 들어
화성충방전 단계에서 예전에 이미 화성충방전을 거친 셀이나 심지어 최종검사까지
끝난 셀까지 다시 후보로 뜨는 문제가 있었다.
→ **"정확히 바로 직전 공정에서 나온 것만 인정"**하는 `predecessors` 제약을
레시피에 추가해서 해결. 스태킹처럼 투입 재료가 2종류(재공품+원자재)인 경우엔
재공품만 직전공정 제약을 걸고, 원자재는 제약 없이 열어두는 식으로 세분화했다.

### 투입 재료 중 하나만 재고가 없어도 등록 폼이 그냥 열려버림
스태킹은 양극판+분리막 둘 다 필요한데, 양극판 재고가 0이어도 분리막만 있으면
폼이 열려서 사용자가 뭐가 문제인지 알기 어려웠다.
→ 카테고리별로 재고 유무를 따로 확인해서, 부족한 품목명을 정확히 짚어주는
경고 문구(`missing_input_categories`)를 추가했다.

### 투입 LOT 재고보다 많은 양을 넣어도 그냥 저장됨
생산실적 등록 로직이 투입수량 > 0인지만 확인하고, 실제 남은 재고량을 넘는지는
확인하지 않았다. 잔여수량이 음수로 떨어질 수 있는 버그였다.
→ 저장 트랜잭션 맨 앞에서 각 투입 LOT의 실제 잔여수량을 먼저 조회해서 초과분을
막도록 수정. 걸리면 아무 것도 저장되지 않고(트랜잭션 롤백) 에러 메시지만 뜬다.

### 극판/조립/화성 세 페이지에 거의 동일한 규칙 코드가 복사돼 있었음
공정별 투입/산출 규칙, 결과수량 계산, LOT번호 생성 로직이 세 파일에 거의
똑같이 중복돼 있어서, 하나를 고치면 나머지 두 곳도 일일이 찾아 고쳐야 했다.
→ `src/process_rules.py` 하나로 통합. `process_code`가 스키마 전체에서
유일한 값이라는 걸 이용해서, 12개 공정 규칙을 딕셔너리 하나로 관리하게 했다.

## 3단계 — LOT 번호 체계 시행착오

셀(CELL) LOT 번호를 어떻게 붙일지 세 번 바뀌었다.
1. 처음엔 `CELL-SN-2026072201`(날짜+순번 붙여쓰기) — 가독성이 떨어짐
2. `CELL-SN-{날짜}-{순번}`으로 하이픈 추가 — 그런데 이건 "몇 번째 셀인지"는
   알아도 "어느 공정에서 나왔는지"는 LOT번호만 봐서 알 수 없었음
3. 최종적으로 `CELL-SN-{공정영문태그}-{날짜}-{그 공정·그날 순번}`
   (예: `CELL-SN-STACK-260805-01`)로 확정 — 공정명을 prefix에 포함시키니
   순번 계산 자체가 자동으로 공정별로 나뉘는 부수효과도 얻었다.

## 4단계 — 대시보드 공정 파이프라인 실시간화

### "몇 개 공정을 완료했는지" 세는 방식의 함정
처음엔 "이 작업지시에서 이 공정을 한 번이라도 수행했는가"로 완료 여부를
판단했다. 그런데 오늘 새로 믹싱을 시작해도, 예전에 이미 끝낸 코팅~최종검사가
계속 "완료"로 표시되는 문제가 있었다. "오늘 새로 시작한 3번째 셀"과
"어제 이미 끝난 1·2번째 셀"을 구분하지 못했기 때문이다.
→ 공정별로 따로 비교하지 않고, **"이 작업지시에서 가장 최근에 시작된 실적이
몇 번째 공정인지" 하나만 먼저 찾고**, 그 위치보다 앞은 완료·그 자신은 시간
비교·뒤는 대기로 판정하는 방식으로 다시 설계했다.

## 5단계 — 자잘하지만 자주 겪은 실수들

- `keys()`를 `key()`로, `==`를 `=`로 잘못 쓴 타이핑 실수
- 위젯에 `value=datetime.now()`처럼 매번 바뀌는 값을 `key` 없이 넘기면,
  다른 위젯을 조작할 때마다 값이 "지금 시각"으로 리셋되는 문제
  → `st.session_state`에 값을 한 번만 저장하고 `key`를 명시해서 해결
- `st.Page()`가 자기 자신을 가리켜서 무한 재귀(`RecursionError`)가 난 것
  → 진입점 파일(`main.py`)과 페이지 내용 파일을 분리해서 해결
- 함수를 두 번 정의해서 나중 버전이 조용히 앞 버전을 덮어써버린 것
  (파이썬은 에러 없이 그냥 마지막 정의만 씀 — 디버깅하며 배운 점)

## 돌아보며

이 프로젝트에서 가장 크게 배운 건, **"일단 되게 만드는 것"과 "제대로 되게
만드는 것" 사이의 간극**이었다. 특히 LOT 필터링 문제(2단계)는 겉보기엔
잘 동작하는 것처럼 보였는데, 실제로 여러 작업지시·여러 날짜의 데이터가
쌓이고 나서야 허점이 드러났다. 데이터가 하나뿐일 때는 안 보이던 버그가
데이터가 늘어나면서 드러나는 경험을 여러 번 했다.